version: '3.7'
services:
  api:
    container_name: mfa_chatbot_api
    restart: always
    build: .
    ports:
     - ${EXTERNAL_PORT:-5322}:${INTERNAL_PORT:-5000}
    volumes:
     - .:/code
    environment:
      - APP_PORT=${INTERNAL_PORT:-5000}
      - TZ=${TZ:-America/Mazatlan}
